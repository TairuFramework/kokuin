# kokuin

刻印 -- identity layer of the stack (token, capability, and per-environment
keystores: browser, node, electron, expo, deterministic, ledger-device).

Depends downward on `@sozai`; consumed by `@enkaku` and `@kumiai`.
See the [stack overview](https://github.com/TairuFramework/kigu/blob/main/docs/stack.md)
for context.
