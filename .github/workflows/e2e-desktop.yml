name: Desktop E2E
on:
  push:
    branches: [main]
  pull_request:
jobs:
  test:
    uses: TairuFramework/kigu/.github/workflows/e2e-desktop.yml@main
    with:
      working-directory: tests/e2e-electron
      node-version: '24'
