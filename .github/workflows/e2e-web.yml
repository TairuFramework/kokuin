name: Web E2E
on:
  push:
    branches: [main]
  pull_request:
jobs:
  test:
    uses: TairuFramework/kigu/.github/workflows/e2e-web.yml@main
    with:
      working-directory: tests/e2e-web
      node-version: '24'
